services:
  syllabus:
    ports:
      - "8081:8080"
    # image: michaeldvinci/syllabus:latest
    image: local-syllabus:0.25
    volumes:
      - ./config:/config
      - ./data:/data
    # environment:
      # Application Settings (override YAML config if set)
      # SYLLABUS_AUTO_REFRESH_INTERVAL: "6"  # Hours between auto-refreshes
      # SYLLABUS_DEFAULT_WORKERS: "4"        # Number of scraper workers
      # SYLLABUS_SERVER_PORT: "8080"         # Server port (or use PORT)
      # PORT: "8080"                         # Standard port env var (alternative)
      # SYLLABUS_CACHE_TIMEOUT: "6"          # Cache timeout in hours
      # SYLLABUS_LOG_LEVEL: "info"           # Log level: debug, info, warn, error